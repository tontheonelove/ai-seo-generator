/**
 * ================================================================
 * Footer — ท้ายเพจของทุกหน้า (เรียบง่าย ไม่แย่งความสนใจ)
 * ================================================================
 */
export function Footer() {
  return (
    <footer className="border-t border-border/50 py-6">
      <p className="text-center text-xs text-muted-foreground">
        SEO EZ — ค้นหาคีย์เวิร์ดง่าย ๆ ในคลิกเดียว ⚡ ขับเคลื่อนด้วย AI ผ่าน
        OpenRouter
      </p>
    </footer>
  );
}